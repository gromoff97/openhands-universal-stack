name: ${STACK_NAME:-openhands-support}

services:
  # Build job: produces the custom OpenHands sandbox image with distill installed.
  runtime-build:
    build:
      context: .
      dockerfile_inline: |
        FROM ghcr.io/openhands/agent-server:1.12.0-python

        ARG DISTILL_MODEL=qwen3.5:2b
        ARG DISTILL_TIMEOUT_MS=90000
        ARG DISTILL_HOST=http://host.docker.internal:11435

        USER root

        RUN npm install -g @samuelfaj/distill@1.4.0

        USER openhands

        RUN HOME=/home/openhands distill config host "$$DISTILL_HOST" \
            && HOME=/home/openhands distill config model "$$DISTILL_MODEL" \
            && HOME=/home/openhands distill config thinking false \
            && HOME=/home/openhands distill config timeout-ms "$$DISTILL_TIMEOUT_MS"
      args:
        DISTILL_HOST: "http://host.docker.internal:${OLLAMA_HOST_PORT:-11435}"
        DISTILL_MODEL: "${DISTILL_OLLAMA_MODEL:-qwen3.5:2b}"
        DISTILL_TIMEOUT_MS: "${DISTILL_TIMEOUT_MS:-90000}"
    image: ${STACK_NAME:-openhands-support}-runtime:latest
    entrypoint: ["true"]

  # Long-lived services.
  chatmock:
    build:
      context: .
      dockerfile_inline: |
        FROM python:3.13-slim

        ARG CHATMOCK_COMMIT=baf312a02e54dd62ab74be485759da5ee9ccf226

        WORKDIR /src

        RUN apt-get update \
            && apt-get install -y --no-install-recommends git ca-certificates \
            && rm -rf /var/lib/apt/lists/*

        RUN git clone https://github.com/RayBytes/ChatMock.git . \
            && git checkout "$$CHATMOCK_COMMIT"

        COPY scripts/patch_chatmock_responses.py /tmp/patch_chatmock_responses.py
        RUN python /tmp/patch_chatmock_responses.py

        RUN pip install --no-cache-dir .

        ENV CHATGPT_LOCAL_HOME=/config
        ENV PYTHONUNBUFFERED=1

        VOLUME ["/config"]

        ENTRYPOINT ["chatmock"]
    image: ${STACK_NAME:-openhands-support}-chatmock:latest
    command:
      [
        "serve",
        "--host",
        "0.0.0.0",
        "--port",
        "5000",
        "--verbose",
        "--debug-model",
        "${CHATMOCK_MODEL:-gpt-5.1-codex-max}",
        "--reasoning-effort",
        "${CHATMOCK_REASONING_EFFORT:-low}",
        "--reasoning-summary",
        "${CHATMOCK_REASONING_SUMMARY:-none}",
      ]
    ports:
      - "5000:5000"
    volumes:
      - chatmock-state:/config
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5000/v1/models', timeout=5).read()",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    ports:
      - "${OLLAMA_HOST_PORT:-11435}:11434"
    volumes:
      - ollama-data:/root/.ollama
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped

  memory-mcp:
    image: supercorp/supergateway:3.4.3
    environment:
      MEMORY_FILE_PATH: /data/memory.jsonl
    command: >
      --stdio "npx -y @modelcontextprotocol/server-memory@2026.1.26"
        --outputTransport streamableHttp
        --port 8000
        --streamableHttpPath /mcp
        --healthEndpoint /healthz
    volumes:
      - memory-data:/data
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:8000/healthz').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped

  context7-mcp:
    build:
      context: .
      dockerfile_inline: |
        FROM node:22-slim

        RUN npm install -g @upstash/context7-mcp@2.1.4

        ENTRYPOINT ["context7-mcp"]
        CMD ["--transport", "http", "--port", "3000"]
    image: ${STACK_NAME:-openhands-support}-context7-mcp:latest
    environment:
      CONTEXT7_API_KEY: ${CONTEXT7_API_KEY:-}
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000/mcp').then(r=>process.exit(r.status===405||r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    restart: unless-stopped

  # One-shot jobs.
  chatmock-login:
    image: ${STACK_NAME:-openhands-support}-chatmock:latest
    profiles:
      - login
    volumes:
      - chatmock-state:/config
    ports:
      - "1455:1455"
    command: ["login"]
    stdin_open: true
    tty: true

  ollama-pull:
    image: ollama/ollama:latest
    depends_on:
      ollama:
        condition: service_healthy
    environment:
      OLLAMA_HOST: http://ollama:11434
    command: ["pull", "${DISTILL_OLLAMA_MODEL:-qwen3.5:2b}"]

  openhands:
    image: docker.openhands.dev/openhands/openhands:latest
    depends_on:
      runtime-build:
        condition: service_completed_successfully
      ollama-pull:
        condition: service_completed_successfully
      chatmock:
        condition: service_healthy
      context7-mcp:
        condition: service_healthy
      memory-mcp:
        condition: service_healthy
    ports:
      - "${OPENHANDS_PORT:-3001}:3000"
    environment:
      AGENT_SERVER_IMAGE_REPOSITORY: ${STACK_NAME:-openhands-support}-runtime
      AGENT_SERVER_IMAGE_TAG: latest
      OH_SECRET_KEY: ${OH_SECRET_KEY:-openhands-universal-stack-dev-secret}
      OH_AGENT_SERVER_ENV: >-
        {"OH_ALLOW_CORS_ORIGINS_1":"http://localhost:${OPENHANDS_PORT:-3001}","OH_ALLOW_CORS_ORIGINS_2":"http://127.0.0.1:${OPENHANDS_PORT:-3001}"}
      OH_SANDBOX_HOST_PORT: ${OPENHANDS_PORT:-3001}
      OH_WEB_URL: http://host.docker.internal:${OPENHANDS_PORT:-3001}
      SANDBOX_HOST_PORT: ${OPENHANDS_PORT:-3001}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${HOME}/.openhands:/.openhands
    restart: unless-stopped

volumes:
  chatmock-state:
  ollama-data:
  memory-data:
